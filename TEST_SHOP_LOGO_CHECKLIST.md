# قائمة اختبار نظام شعار المحل

استخدم هذه القائمة للتأكد من أن نظام الشعار يعمل بشكل صحيح.

---

## ✅ الاختبار 1: رفع شعار جديد (5 دقائق)

### الخطوات:

- [ ] 1. افتح التطبيق
- [ ] 2. اذهب إلى **الإعدادات** → **إعدادات المحل**
- [ ] 3. اضغط على **اختيار من المعرض**
- [ ] 4. اختر صورة شعار (يفضل مربعة، أقل من 5 MB)
- [ ] 5. معاينة الصورة تظهر في الشاشة
- [ ] 6. في قسم "الشعار في السندات"، اختر **استخدام الشعار المرفوع**
- [ ] 7. اضغط على **حفظ**

### النتيجة المتوقعة:

- [ ] رسالة نجاح: "تم حفظ الإعدادات والشعار بنجاح"
- [ ] الشعار يظهر في معاينة الإعدادات
- [ ] لا توجد أخطاء في console

### Console Logs المتوقعة:

```
[logoService] Starting upload for: file:///...
[logoService] File read successfully, size: X.XX MB
[logoService] Upload successful
[AuthContext] Settings upserted successfully
```

### إذا فشل:

- [ ] افحص console logs للأخطاء
- [ ] تحقق من حجم الملف (< 5 MB)
- [ ] تحقق من نوع الملف (JPG, PNG, WEBP)
- [ ] راجع `SHOP_LOGO_FIX_SUMMARY.md` → "استكشاف الأخطاء"

---

## ✅ الاختبار 2: التحقق من قاعدة البيانات (2 دقيقة)

### الخطوات:

- [ ] 1. افتح Supabase Dashboard → SQL Editor
- [ ] 2. شغّل هذا الاستعلام:

```sql
SELECT id, shop_logo, selected_receipt_logo
FROM app_settings
WHERE id = '00000000-0000-0000-0000-000000000000';
```

### النتيجة المتوقعة:

- [ ] السجل موجود
- [ ] `shop_logo` يحتوي على رابط Supabase Storage مثل:
  ```
  https://[PROJECT].supabase.co/storage/v1/object/public/shop-logos/logos/default_1234567890.jpg
  ```
- [ ] `selected_receipt_logo` يحتوي على نفس الرابط أو `null`

### إذا فشل:

- [ ] السجل غير موجود → أنشئه:
  ```sql
  INSERT INTO app_settings (id, shop_name)
  VALUES ('00000000-0000-0000-0000-000000000000', 'الترف للحوالات المالية')
  ON CONFLICT (id) DO NOTHING;
  ```
- [ ] `shop_logo` فارغ → أعد الاختبار 1

---

## ✅ الاختبار 3: فتح الشعار في المتصفح (1 دقيقة)

### الخطوات:

- [ ] 1. انسخ رابط `shop_logo` من قاعدة البيانات
- [ ] 2. الصق الرابط في متصفح جديد
- [ ] 3. اضغط Enter

### النتيجة المتوقعة:

- [ ] الصورة تفتح بشكل صحيح في المتصفح
- [ ] لا توجد رسالة "Access Denied" أو 404

### إذا فشل:

- [ ] خطأ 404 → الملف غير موجود في Storage
- [ ] Access Denied → مشكلة في Storage policies
- [ ] شغّل `VERIFY_SHOP_LOGO_SETUP.sql` للتحقق من السياسات

---

## ✅ الاختبار 4: الشعار يظهر في السندات (3 دقائق)

### الخطوات:

- [ ] 1. بعد رفع الشعار (الاختبار 1)
- [ ] 2. اذهب إلى **العملاء** → اختر عميل
- [ ] 3. أنشئ حركة جديدة (تحويل/استلام/تسليم)
- [ ] 4. املأ البيانات واحفظ
- [ ] 5. افتح السند من قائمة الحركات
- [ ] 6. اضغط على **مشاركة PDF** أو **طباعة**

### النتيجة المتوقعة:

- [ ] السند يُنشأ بنجاح
- [ ] الشعار المرفوع يظهر في هيدر السند (في الوسط)
- [ ] الشعار واضح ومقروء

### Console Logs المتوقعة:

```
[logoHelper] getReceiptLogoBase64 called
[logoHelper] Found uploaded logo in Supabase Storage
[logoHelper] Downloading logo from Storage: https://...
[logoHelper] Successfully converted uploaded logo to base64, length: XXXXX
```

### إذا فشل:

- [ ] الشعار لا يظهر → افحص console logs
- [ ] `[logoHelper] User selected DEFAULT logo` → غيّر الإعداد في "إعدادات المحل"
- [ ] `[logoHelper] Error downloading` → مشكلة في الاتصال أو Storage policies

---

## ✅ الاختبار 5: إعادة تشغيل التطبيق (2 دقيقة)

### الخطوات:

- [ ] 1. أغلق التطبيق بالكامل (kill من App Switcher)
- [ ] 2. أعد فتح التطبيق
- [ ] 3. ادخل PIN
- [ ] 4. اذهب إلى **إعدادات المحل**

### النتيجة المتوقعة:

- [ ] الشعار المرفوع يظهر في معاينة الإعدادات
- [ ] "استخدام الشعار المرفوع" محدد

### الخطوات 2:

- [ ] 5. أنشئ سند جديد
- [ ] 6. الشعار يظهر في السند

### إذا فشل:

- [ ] الشعار اختفى → المشكلة في حفظ قاعدة البيانات
- [ ] افحص RLS policies في `VERIFY_SHOP_LOGO_SETUP.sql`

---

## ✅ الاختبار 6: استخدام الشعار الافتراضي (2 دقيقة)

### الخطوات:

- [ ] 1. اذهب إلى **إعدادات المحل**
- [ ] 2. في قسم "الشعار في السندات"، اختر **استخدام الشعار الافتراضي**
- [ ] 3. احفظ
- [ ] 4. أنشئ سند جديد

### النتيجة المتوقعة:

- [ ] السند يُنشأ بنجاح
- [ ] الشعار الافتراضي (`assets/images/logo_1.png`) يظهر في السند
- [ ] لا يظهر الشعار المرفوع

### Console Logs المتوقعة:

```
[logoHelper] User selected DEFAULT logo, using bundled logo
[logoHelper] Loading bundled logo from assets...
[logoHelper] Successfully loaded bundled logo
```

### إذا فشل:

- [ ] لا يظهر أي شعار → مشكلة في assets
- [ ] يظهر الشعار المرفوع → الإعداد لم يُحفظ

---

## ✅ الاختبار 7: بناء APK (اختياري - 30 دقيقة)

**ملاحظة**: هذا الاختبار للتأكد من أن الشعار يعمل في نسخة الإنتاج، وليس فقط في التطوير.

### الخطوات:

- [ ] 1. ابني التطبيق كـ APK:
  ```bash
  eas build --platform android --profile production
  ```
- [ ] 2. حمّل APK بعد انتهاء البناء
- [ ] 3. ثبّت APK على جهاز Android حقيقي
- [ ] 4. افتح التطبيق وارفع شعار جديد
- [ ] 5. أنشئ سند

### النتيجة المتوقعة:

- [ ] الشعار يظهر في السندات في APK
- [ ] تحميل الشعار من Storage يعمل
- [ ] التحويل إلى base64 يعمل

### إذا فشل:

- [ ] راجع `SHOP_LOGO_COMPLETE_GUIDE.md` → "استكشاف الأخطاء" → "الشعار يظهر في التطوير ولكن ليس في APK"

---

## 📊 ملخص النتائج

| الاختبار | النتيجة | ملاحظات |
|---------|---------|---------|
| 1. رفع شعار جديد | ⬜ نجح / ❌ فشل | |
| 2. التحقق من قاعدة البيانات | ⬜ نجح / ❌ فشل | |
| 3. فتح الشعار في المتصفح | ⬜ نجح / ❌ فشل | |
| 4. الشعار في السندات | ⬜ نجح / ❌ فشل | |
| 5. إعادة تشغيل التطبيق | ⬜ نجح / ❌ فشل | |
| 6. الشعار الافتراضي | ⬜ نجح / ❌ فشل | |
| 7. بناء APK | ⬜ نجح / ❌ فشل / ⬜ لم يُجرَّ | |

**إذا نجحت جميع الاختبارات 1-6:** ✅ النظام يعمل بشكل صحيح!

**إذا فشل أي اختبار:** راجع التعليمات في كل اختبار أو راجع `SHOP_LOGO_COMPLETE_GUIDE.md`

---

## 🐛 استكشاف الأخطاء السريع

### خطأ: "فشل حفظ الإعدادات في قاعدة البيانات"

```sql
-- تحقق من RLS policies
SELECT policyname, cmd, roles
FROM pg_policies
WHERE tablename = 'app_settings';

-- إذا لم تسمح لـ anon، أنشئ السياسة:
CREATE POLICY "Allow anon and authenticated users full access to app_settings"
  ON app_settings FOR ALL
  TO anon, authenticated
  USING (true) WITH CHECK (true);
```

### خطأ: "الشعار لا يظهر في السندات"

```sql
-- تحقق من selected_receipt_logo
SELECT selected_receipt_logo FROM app_settings;

-- إذا كانت 'DEFAULT'، غيّرها في الإعدادات
-- إذا كانت null أو رابط، تحقق من Storage policies
```

### خطأ: "حجم الملف كبير جداً"

- استخدم [TinyPNG](https://tinypng.com/) لضغط الصورة
- أو اختر صورة أصغر (< 5 MB)

---

**تاريخ آخر تحديث**: 12 يناير 2026
