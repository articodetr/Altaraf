# دليل بناء التطبيق على أندرويد

## المتطلبات الأساسية

قبل البدء، تأكد من توفر:
1. حساب Expo
2. Node.js و npm مثبتين
3. جهاز أندرويد فعلي أو محاكي (Android Studio)

---

## طريقة 1: استخدام Expo Go (للاختبار السريع)

### الخطوات:

1. **تثبيت Expo Go على جهاز أندرويد**
   - افتح Google Play Store
   - ابحث عن "Expo Go"
   - قم بتثبيت التطبيق

2. **تشغيل خادم التطوير**
   ```bash
   npm run dev
   ```

3. **مسح رمز QR**
   - افتح Expo Go على جهازك
   - اضغط على "Scan QR code"
   - امسح الرمز الظاهر في Terminal

### ⚠️ ملاحظة مهمة:
**لا يمكن اختبار ميزات الطباعة وتصدير PDF في Expo Go!**
Expo Go لا يدعم الوحدات الأصلية مثل expo-print. للاختبار الكامل، استخدم الطريقة 2 أو 3.

---

## طريقة 2: Development Build (موصى بها للاختبار)

### الخطوات:

1. **تثبيت EAS CLI**
   ```bash
   npm install -g eas-cli
   ```

2. **تسجيل الدخول إلى Expo**
   ```bash
   eas login
   ```

3. **إعداد المشروع**
   ```bash
   eas build:configure
   ```

4. **بناء نسخة Development**
   ```bash
   eas build --profile development --platform android
   ```

5. **تثبيت التطبيق على الجهاز**
   - بعد اكتمال البناء، ستحصل على رابط تحميل
   - افتح الرابط على جهاز أندرويد
   - قم بتثبيت التطبيق

6. **تشغيل خادم التطوير**
   ```bash
   npm run dev
   ```

7. **فتح التطبيق**
   - افتح التطبيق المثبت على جهازك
   - سيتصل تلقائياً بخادم التطوير

### المزايا:
- ✅ يدعم جميع الميزات الأصلية
- ✅ يمكن اختبار الطباعة وتصدير PDF
- ✅ Hot reload متاح
- ✅ مجاني تماماً

---

## طريقة 3: Production Build (للنشر)

### الخطوات:

1. **بناء نسخة Production**
   ```bash
   eas build --platform android
   ```

2. **اختيار نوع البناء**
   - سيسألك عن نوع البناء:
     - `apk`: ملف APK (للتوزيع اليدوي)
     - `aab`: Android App Bundle (للنشر على Google Play)

3. **تحميل وتثبيت**
   - بعد اكتمال البناء، حمل الملف
   - ثبته على جهازك (APK)
   - أو ارفعه على Google Play Console (AAB)

### ملاحظة:
هذه النسخة لا تتصل بخادم التطوير وتعمل بشكل مستقل تماماً.

---

## طريقة 4: البناء المحلي (للمطورين المتقدمين)

### المتطلبات:
- Android Studio
- Android SDK
- Java Development Kit (JDK)

### الخطوات:

1. **تثبيت المتطلبات**
   ```bash
   # تثبيت expo-dev-client
   npm install expo-dev-client
   ```

2. **تشغيل البناء المحلي**
   ```bash
   npx expo run:android
   ```

3. **البناء سيحدث تلقائياً**
   - سيتم إنشاء مجلد `android/`
   - سيتم بناء التطبيق محلياً
   - سيتم تثبيته على الجهاز/المحاكي المتصل

### المزايا:
- ✅ بناء سريع (محلي)
- ✅ تحكم كامل في إعدادات البناء
- ✅ لا يتطلب اتصال إنترنت (بعد التثبيت الأولي)

---

## اختبار ميزات الطباعة

بعد تثبيت التطبيق (باستخدام الطريقة 2 أو 3 أو 4):

### 1. اختبار طباعة الإيصال
1. افتح تفاصيل عميل
2. اضغط على حركة مالية
3. اختر "طباعة السند"
4. تحقق من:
   - ✅ عرض الخطوط العربية بشكل صحيح
   - ✅ ظهور الألوان والتدرجات
   - ✅ إمكانية المشاركة عبر التطبيقات

### 2. اختبار كشف الحساب
1. افتح تفاصيل عميل
2. اضغط على "طباعة PDF"
3. تحقق من:
   - ✅ عرض جميع الحركات بشكل صحيح
   - ✅ التنسيق والجداول صحيحة
   - ✅ إمكانية الحفظ والمشاركة

### 3. اختبار تقرير الديون
1. افتح "تقرير الديون الشامل"
2. اضغط على أيقونة التحميل
3. تحقق من:
   - ✅ عرض البيانات بشكل صحيح
   - ✅ الجداول منسقة
   - ✅ إمكانية المشاركة

---

## حل المشاكل الشائعة

### مشكلة: "Metro bundler failed to start"
**الحل**:
```bash
npx expo start --clear
```

### مشكلة: "Could not connect to development server"
**الحل**:
- تأكد من أن الجهاز والكمبيوتر على نفس الشبكة
- جرب إعادة تشغيل خادم التطوير
- استخدم Tunnel mode:
  ```bash
  npx expo start --tunnel
  ```

### مشكلة: "Build failed on EAS"
**الحل**:
- تحقق من سجلات البناء على https://expo.dev
- تأكد من صحة ملف app.json
- تحقق من أن جميع التبعيات مثبتة بشكل صحيح

### مشكلة: "App crashes on Android"
**الحل**:
1. تحقق من Logcat:
   ```bash
   adb logcat
   ```
2. تأكد من منح جميع الصلاحيات المطلوبة
3. أعد تثبيت التطبيق

---

## إعدادات إضافية (اختيارية)

### 1. تخصيص أيقونة التطبيق
استبدل الملفات في `assets/images/`:
- `icon.png`: أيقونة التطبيق (1024x1024)
- `favicon.png`: أيقونة الويب

### 2. تخصيص شاشة البداية (Splash Screen)
أضف إعدادات في `app.json`:
```json
{
  "splash": {
    "image": "./assets/splash.png",
    "resizeMode": "contain",
    "backgroundColor": "#ffffff"
  }
}
```

### 3. تخصيص الألوان
عدّل ألوان StatusBar في `app.json`:
```json
{
  "android": {
    "statusBar": {
      "backgroundColor": "#059669",
      "barStyle": "light-content"
    }
  }
}
```

---

## النشر على Google Play Store

### الخطوات:

1. **بناء نسخة Production AAB**
   ```bash
   eas build --platform android --profile production
   ```

2. **إنشاء حساب Google Play Developer**
   - زر https://play.google.com/console/signup
   - رسوم لمرة واحدة: $25

3. **إنشاء تطبيق جديد**
   - اضغط على "Create app"
   - املأ التفاصيل المطلوبة

4. **رفع AAB**
   - اذهب إلى Production > Create new release
   - ارفع ملف AAB
   - أكمل جميع المعلومات المطلوبة

5. **مراجعة النشر**
   - املأ سياسة الخصوصية
   - أضف صور التطبيق
   - انتظر المراجعة (عادة 1-3 أيام)

---

## الموارد المفيدة

- [Expo Documentation](https://docs.expo.dev/)
- [EAS Build](https://docs.expo.dev/build/introduction/)
- [Google Play Console](https://play.google.com/console)
- [Android Developers](https://developer.android.com/)

---

## الخلاصة

استخدم **الطريقة 2 (Development Build)** للاختبار الشامل لجميع الميزات، بما في ذلك الطباعة وتصدير PDF. عندما تكون جاهزاً للنشر، استخدم **الطريقة 3 (Production Build)**.

**حظاً موفقاً!** 🚀
