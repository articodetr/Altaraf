# دليل قوالب رسائل الواتساب

## نظرة عامة

تم إضافة ميزة تخصيص رسائل الواتساب التي يتم إرسالها للعملاء. يمكنك الآن تعديل القوالب من الإعدادات لتخصيص الرسائل حسب احتياجاتك.

## الوصول إلى الإعدادات

1. انتقل إلى تبويب **الإعدادات**
2. اضغط على **قوالب رسائل الواتساب**
3. سترى شاشة تحتوي على قالبين:
   - قالب رسالة كشف الحساب
   - قالب رسالة تفاصيل الحوالة

## أنواع القوالب

### 1. قالب رسالة كشف الحساب

يستخدم هذا القالب عند الضغط على زر الواتساب من صفحة تفاصيل العميل.

**المتغيرات المتاحة:**
- `{customer_name}` - اسم العميل
- `{account_number}` - رقم حساب العميل
- `{date}` - التاريخ الحالي (يتم تنسيقه تلقائياً)
- `{balance}` - الرصيد الحالي مع تفاصيل العملات
- `{shop_name}` - اسم المحل من الإعدادات
- `{shop_phone}` - رقم هاتف المحل من الإعدادات

**مثال على القالب الافتراضي:**
```
مرحباً {customer_name}،
رقم الحساب: {account_number}
التاريخ: {date}

{balance}
```

### 2. قالب رسالة تفاصيل الحوالة

يستخدم هذا القالب عند الضغط على زر الواتساب من صفحة تفاصيل الحوالة.

**المتغيرات المتاحة:**
- `{customer_name}` - اسم العميل
- `{transaction_number}` - رقم الحوالة
- `{amount_sent}` - المبلغ المرسل (رقم مع خانتين عشريتين)
- `{amount_received}` - المبلغ المستلم (رقم مع خانتين عشريتين)
- `{currency_sent}` - عملة الإرسال
- `{currency_received}` - عملة الاستلام
- `{date}` - التاريخ الحالي
- `{shop_name}` - اسم المحل
- `{shop_phone}` - رقم هاتف المحل

**مثال على القالب الافتراضي:**
```
مرحباً {customer_name}،

سند الحوالة رقم: {transaction_number}

المبلغ المرسل: {amount_sent} {currency_sent}
المبلغ المستلم: {amount_received} {currency_received}

شكراً لثقتكم بنا
{shop_name}
```

## كيفية تعديل القوالب

1. في شاشة **قوالب رسائل الواتساب**:
   - اكتب القالب الذي تريده في الحقل المخصص
   - استخدم المتغيرات بين أقواس معقوفة `{}`
   - يمكنك إضافة نص ثابت بجانب المتغيرات
   - يمكنك حذف أي متغير لا تحتاجه

2. **معاينة الرسالة**:
   - اضغط على زر "معاينة الرسالة" لرؤية كيف ستظهر الرسالة
   - المعاينة تستخدم بيانات تجريبية لتوضيح الشكل النهائي

3. **حفظ التغييرات**:
   - اضغط على زر "حفظ التغييرات" لحفظ القوالب الجديدة
   - سيتم تطبيق القوالب الجديدة فوراً

4. **استعادة الافتراضي**:
   - إذا أردت العودة للقوالب الافتراضية، اضغط على "استعادة الافتراضي"
   - سيُطلب منك التأكيد قبل استعادة القيم الافتراضية

## نصائح مهمة

### ✅ افعل:
- استخدم المتغيرات بين أقواس معقوفة `{}`
- تأكد من كتابة اسم المتغير بشكل صحيح تماماً
- استخدم أسطر جديدة لتنظيم الرسالة
- اضغط على المعاينة للتأكد من الشكل النهائي
- احفظ التغييرات بعد التعديل

### ❌ لا تفعل:
- لا تترك القالب فارغاً
- لا تستخدم أقواس عادية `()` أو مربعة `[]` للمتغيرات
- لا تضف مسافات داخل أقواس المتغيرات `{ customer_name }` ❌
- لا تنسَ حفظ التغييرات قبل الخروج

## أمثلة على قوالب مخصصة

### مثال 1: قالب كشف حساب بسيط
```
السلام عليكم {customer_name}

رصيدك: {balance}

{shop_name}
هاتف: {shop_phone}
```

### مثال 2: قالب كشف حساب تفصيلي
```
عزيزي العميل {customer_name}،

حسابكم رقم: {account_number}
تاريخ الاستعلام: {date}

═══════════════
{balance}
═══════════════

للاستفسار: {shop_phone}
{shop_name}
```

### مثال 3: قالب حوالة احترافي
```
تحية طيبة {customer_name}،

✅ تم تسجيل الحوالة بنجاح

📋 رقم السند: {transaction_number}
💵 المبلغ المرسل: {amount_sent} {currency_sent}
💰 المبلغ المستلم: {amount_received} {currency_received}

نشكركم على ثقتكم
{shop_name} - {shop_phone}
```

## التقنيات المستخدمة

### الملفات المضافة:
1. **Migration:** `add_whatsapp_message_templates.sql`
   - يضيف عمودين جديدين إلى جدول `app_settings`

2. **Helper:** `utils/whatsappTemplateHelper.ts`
   - يحتوي على دوال معالجة القوالب
   - يستبدل المتغيرات بالقيم الفعلية

3. **Screen:** `app/whatsapp-templates.tsx`
   - شاشة تعديل القوالب مع معاينة فورية

4. **Updates:**
   - تحديث `app/(tabs)/settings.tsx` لإضافة رابط القوالب
   - تحديث `app/customer-details.tsx` لاستخدام القالب المخصص
   - تحديث `app/transaction-details.tsx` لاستخدام القالب المخصص
   - تحديث `types/database.ts` لإضافة الحقول الجديدة

## الأسئلة الشائعة

**س: هل يمكنني حذف بعض المتغيرات من القالب؟**
ج: نعم، يمكنك حذف أي متغير لا تحتاجه من القالب.

**س: هل يمكنني إضافة متغيرات خاصة بي؟**
ج: لا، يجب استخدام المتغيرات المتاحة فقط. المتغيرات المخصصة تتطلب تعديلات برمجية.

**س: ماذا يحدث إذا كتبت المتغير بشكل خاطئ؟**
ج: المتغير سيظهر كما هو في الرسالة بدون استبداله بالقيمة الفعلية.

**س: هل التغييرات تؤثر على الرسائل السابقة؟**
ج: لا، التغييرات تطبق فقط على الرسائل الجديدة التي يتم إرسالها بعد الحفظ.

**س: هل يمكنني استخدام إيموجي في القوالب؟**
ج: نعم، يمكنك استخدام إيموجي في أي مكان في القالب.

## الدعم والمساعدة

إذا واجهت أي مشاكل أو كان لديك أسئلة:
- تأكد من كتابة المتغيرات بشكل صحيح
- استخدم ميزة المعاينة للتحقق من الرسالة
- اضغط على "استعادة الافتراضي" إذا حدثت مشاكل
- تأكد من حفظ التغييرات بعد التعديل
